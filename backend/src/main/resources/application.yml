spring:
  application:
    name: energy-news-backend
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/energy_news}
    username: ${SPRING_DATASOURCE_USERNAME:energy_news}
    password: ${SPRING_DATASOURCE_PASSWORD:energy_news}
  jpa:
    hibernate:
      ddl-auto: update

app:
  news-sync:
    cron: "0 0 2 * * *"
    zone: "Asia/Shanghai"
  news-filter:
    keywords:
      - energy storage
      - battery storage
      - BESS
      - grid storage
      - LFP
      - sodium-ion
      - flow battery
