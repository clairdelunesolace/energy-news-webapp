services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: energy_news
      POSTGRES_USER: energy_news
      POSTGRES_PASSWORD: energy_news
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
