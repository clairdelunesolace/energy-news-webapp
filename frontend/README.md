# 储能资讯前端

React、TypeScript 和 Vite 构建的前端项目。

## 开发

请先启动本地 Spring Boot 后端，然后运行：

```powershell
npm install
npm run dev
```

开发服务器通过 Vite 将 `/api` 请求代理到 `http://localhost:8080`。

## 构建

```powershell
npm run build
```
